{
  "name": "node-service-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --testPathIgnorePatterns='.compiled/*'",
    "compile": "babel src test --out-dir .compiled --source-maps --watch --verbose"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.4",
    "@babel/preset-env": "^7.4.4",
    "jest": "^24.7.1",
    "supertest": "^4.0.2"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}